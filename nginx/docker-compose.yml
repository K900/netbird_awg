services:

  nginx:
    container_name: nb-awg-nginx
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
      args:
        RELEASE_VERSION: ${RELEASE_VERSION}
        GITHUB_REPO: ${GITHUB_REPO}
    env_file:
      - .env
    ports:
      - "80:80"
