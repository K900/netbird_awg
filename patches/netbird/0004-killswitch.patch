From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: spikeyspik <24636111+spikeyspik@users.noreply.github.com>
Date: Mon, 27 Jul 2026 00:00:00 +0300
Subject: [PATCH] client: killswitch

diff --git a/client/internal/connect.go b/client/internal/connect.go
index 87126b222..576c31162 100644
--- a/client/internal/connect.go
+++ b/client/internal/connect.go
@@ -70,6 +70,8 @@ type ConnectClient struct {
 	updateManager *updater.Manager
 
 	persistSyncResponse bool
+	beforeClientRestart func(reason string) bool
+	onEngineStarted     func()
 }
 
 func NewConnectClient(
@@ -95,6 +97,11 @@ func (c *ConnectClient) SetUpdateManager(um *updater.Manager) {
 	c.updateManager = um
 }
 
+func (c *ConnectClient) SetRestartHooks(beforeRestart func(reason string) bool, onStarted func()) {
+	c.beforeClientRestart = beforeRestart
+	c.onEngineStarted = onStarted
+}
+
 // Run with main logic.
 func (c *ConnectClient) Run(runningChan chan struct{}, logPath string) error {
 	if androidRunOverride != nil {
@@ -421,6 +428,7 @@ func (c *ConnectClient) run(mobileDependency MobileDependency, runningChan chan
 			UpdateManager:  c.updateManager,
 			ClientMetrics:  c.clientMetrics,
 			MetricsCtx:     c.ctx,
+			BeforeClientRestart: c.beforeClientRestart,
 		}, mobileDependency)
 		engine.SetSyncResponsePersistence(c.persistSyncResponse)
 		c.engine = engine
@@ -445,6 +453,9 @@ func (c *ConnectClient) run(mobileDependency MobileDependency, runningChan chan
 				close(runningChan)
 			}
 		}
+		if c.onEngineStarted != nil {
+			c.onEngineStarted()
+		}
 
 		<-engineCtx.Done()
 
diff --git a/client/internal/dns/local/local.go b/client/internal/dns/local/local.go
index fef35fd41..655ab283b 100644
--- a/client/internal/dns/local/local.go
+++ b/client/internal/dns/local/local.go
@@ -531,13 +531,14 @@ func (d *Resolver) logDNSError(logger *log.Entry, hostname string, qtype uint16,
 // non-mesh upstreams) are never dropped. Non-A/AAAA records pass
 // through untouched.
 //
-// Escape hatch: if filtering would leave the answer empty AND at least
-// one record was filtered, the original list is returned. Better to
-// hand the client a record that may not respond than NXDOMAIN it
-// completely when every proxy peer is offline (the upstream may still
-// be reachable some other way, or the peerstore may be stale).
+// If all A/AAAA answers point at disconnected peers, return a sinkhole
+// answer. This is intentional fail-closed behavior: as long as NetBird
+// is running and the host is known in the local config, traffic must not
+// fall back to a non-NetBird path just because every matching peer is
+// currently unavailable. A positive short-lived answer also avoids browser
+// DoH/Secure DNS fallback that can happen on NXDOMAIN or SERVFAIL.
 func (d *Resolver) filterDisconnectedPeerAnswers(logger *log.Entry, question dns.Question, records []dns.RR) []dns.RR {
-	if len(records) < 2 {
+	if len(records) == 0 {
 		return records
 	}
 	d.mu.RLock()
@@ -566,13 +567,37 @@ func (d *Resolver) filterDisconnectedPeerAnswers(logger *log.Entry, question dns
 		return records
 	}
 	if len(kept) == 0 {
-		logger.Debugf("all %d answers for %s point at disconnected peers; returning the original list", dropped, question.Name)
-		return records
+		logger.Debugf("all %d answers for %s point at disconnected peers; returning sinkhole answer", dropped, question.Name)
+		return sinkholeAnswer(question)
 	}
 	logger.Tracef("dropped %d disconnected-peer answer(s) for %s, returning %d", dropped, question.Name, len(kept))
 	return kept
 }
 
+func sinkholeAnswer(question dns.Question) []dns.RR {
+	header := dns.RR_Header{
+		Name:   question.Name,
+		Rrtype: question.Qtype,
+		Class:  question.Qclass,
+		Ttl:    1,
+	}
+
+	switch question.Qtype {
+	case dns.TypeA:
+		return []dns.RR{&dns.A{
+			Hdr: header,
+			A:   net.IPv4zero,
+		}}
+	case dns.TypeAAAA:
+		return []dns.RR{&dns.AAAA{
+			Hdr:  header,
+			AAAA: net.IPv6zero,
+		}}
+	default:
+		return nil
+	}
+}
+
 // warmLazyPeers triggers lazy-connection wake-up for the peers a resolved
 // answer points at and waits briefly for one to connect, so the caller's first
 // request doesn't race the connection establishment. Warm-up is scoped to
diff --git a/client/internal/engine.go b/client/internal/engine.go
index 617892e43..1e69c6d8d 100644
--- a/client/internal/engine.go
+++ b/client/internal/engine.go
@@ -180,6 +180,7 @@ type EngineServices struct {
 	UpdateManager  *updater.Manager
 	ClientMetrics  *metrics.ClientMetrics
 	MetricsCtx     context.Context
+	BeforeClientRestart func(reason string) bool
 }
 
 // Engine is a mechanism responsible for reacting on Signal and Management stream events and managing connections to the remote peers.
@@ -211,6 +212,7 @@ type Engine struct {
 
 	clientCtx    context.Context
 	clientCancel context.CancelFunc
+	beforeClientRestart func(reason string) bool
 
 	ctx    context.Context
 	cancel context.CancelFunc
@@ -348,6 +350,7 @@ func NewEngine(
 		clientMetrics:      services.ClientMetrics,
 		metricsCtx:         services.MetricsCtx,
 		updateManager:      services.UpdateManager,
+		beforeClientRestart: services.BeforeClientRestart,
 		syncStoreDir:       config.StateDir,
 	}
 	// sessionWatcher keeps the SubscribeStatus consumers in sync with the
@@ -617,7 +620,9 @@ func (e *Engine) Start(netbirdConfig *mgmProto.NetbirdConfig, mgmtURL *url.URL)
 	}
 
 	if filteredDevice := e.wgInterface.GetDevice(); filteredDevice != nil {
-		filteredDevice.SetPanicHandler(e.triggerClientRestart)
+		filteredDevice.SetPanicHandler(func() {
+			e.triggerClientRestart("wireguard device panic")
+		})
 	}
 
 	if err := e.createFirewall(); err != nil {
@@ -703,7 +708,7 @@ func (e *Engine) Start(netbirdConfig *mgmProto.NetbirdConfig, mgmtURL *url.URL)
 
 		if shouldRestart, err := e.wgIfaceMonitor.Start(e.ctx, wgIfaceName); shouldRestart {
 			log.Infof("WireGuard interface monitor: %s, restarting engine", err)
-			e.triggerClientRestart()
+			e.triggerClientRestart("WireGuard interface monitor event")
 		} else if err != nil {
 			log.Warnf("WireGuard interface monitor: %s", err)
 		}
@@ -2372,15 +2377,19 @@ func (e *Engine) RunHealthProbes(ctx context.Context, waitForResult bool) bool {
 // triggerClientRestart triggers a full client restart by cancelling the client context.
 // Note: This does NOT just restart the engine - it cancels the entire client context,
 // which causes the connect client's retry loop to create a completely new engine.
-func (e *Engine) triggerClientRestart() {
+func (e *Engine) triggerClientRestart(reason string) {
 	e.syncMsgMux.Lock()
 	defer e.syncMsgMux.Unlock()
 
 	if e.ctx.Err() != nil {
 		return
 	}
+	if e.beforeClientRestart != nil && !e.beforeClientRestart(reason) {
+		log.Warnf("skipping client restart after %s because route killswitch barrier is unavailable", reason)
+		return
+	}
 
-	log.Info("restarting engine")
+	log.Infof("restarting engine after %s", reason)
 	CtxGetState(e.ctx).Set(StatusConnecting)
 	_ = CtxGetState(e.ctx).Wrap(ErrResetConnection)
 	log.Infof("cancelling client context, engine will be recreated")
@@ -2407,7 +2416,7 @@ func (e *Engine) startNetworkMonitor() {
 		}
 
 		log.Infof("Network monitor: detected network change, triggering client restart")
-		e.triggerClientRestart()
+		e.triggerClientRestart("network monitor event")
 	}()
 }
 
diff --git a/client/internal/routemanager/client/client.go b/client/internal/routemanager/client/client.go
index c691c54f8..101e958e5 100644
--- a/client/internal/routemanager/client/client.go
+++ b/client/internal/routemanager/client/client.go
@@ -334,6 +334,20 @@ func (w *Watcher) shouldSkipRecalculation(newChosenID route.ID, newStatus router
 	return true
 }
 
+func (w *Watcher) shouldKeepCurrentRouteOnNoChoice(rsn reason) bool {
+	if w.currentChosen == nil {
+		return false
+	}
+	if rsn != reasonPeerUpdate && rsn != reasonRouteUpdate {
+		return false
+	}
+	current, ok := w.routes[w.currentChosen.ID]
+	if !ok {
+		return false
+	}
+	return w.currentChosen.Equal(current)
+}
+
 func (w *Watcher) recalculateRoutes(rsn reason, routerPeerStatuses map[route.ID]routerPeerStatus) error {
 	newChosenID, newStatus := w.getBestRouteFromStatuses(routerPeerStatuses)
 
@@ -342,6 +356,10 @@ func (w *Watcher) recalculateRoutes(rsn reason, routerPeerStatuses map[route.ID]
 		if w.currentChosen == nil {
 			return nil
 		}
+		if w.shouldKeepCurrentRouteOnNoChoice(rsn) {
+			log.Warnf("keeping current routing peer %s for [%v]: no routing peers are currently available", w.currentChosen.Peer, w.handler)
+			return nil
+		}
 
 		if err := w.removeAllowedIPs(w.currentChosen, rsn); err != nil {
 			return fmt.Errorf("remove obsolete: %w", err)
diff --git a/client/internal/routemanager/dnsinterceptor/handler.go b/client/internal/routemanager/dnsinterceptor/handler.go
index f92300bfd..a0dfa83ff 100644
--- a/client/internal/routemanager/dnsinterceptor/handler.go
+++ b/client/internal/routemanager/dnsinterceptor/handler.go
@@ -283,15 +283,50 @@ func (d *DnsInterceptor) ServeDNS(w dns.ResponseWriter, r *dns.Msg) {
 }
 
 func (d *DnsInterceptor) writeDNSError(w dns.ResponseWriter, r *dns.Msg, logger *log.Entry, reason string) {
-	logger.Warnf("failed to query upstream for domain=%s: %s", r.Question[0].Name, reason)
+	logger.Warnf("failed to query upstream for domain=%s: %s; returning sinkhole answer", r.Question[0].Name, reason)
 
-	resp := new(dns.Msg)
-	resp.SetRcode(r, dns.RcodeServerFailure)
+	resp := sinkholeResponse(r)
+	if resp == nil {
+		resp = new(dns.Msg)
+		resp.SetRcode(r, dns.RcodeServerFailure)
+	}
 	if err := w.WriteMsg(resp); err != nil {
 		logger.Errorf("failed to write DNS error response: %v", err)
 	}
 }
 
+func sinkholeResponse(r *dns.Msg) *dns.Msg {
+	if len(r.Question) == 0 {
+		return nil
+	}
+
+	question := r.Question[0]
+	header := dns.RR_Header{
+		Name:   question.Name,
+		Rrtype: question.Qtype,
+		Class:  question.Qclass,
+		Ttl:    1,
+	}
+
+	resp := new(dns.Msg)
+	resp.SetReply(r)
+	switch question.Qtype {
+	case dns.TypeA:
+		resp.Answer = []dns.RR{&dns.A{
+			Hdr: header,
+			A:   net.IPv4zero,
+		}}
+	case dns.TypeAAAA:
+		resp.Answer = []dns.RR{&dns.AAAA{
+			Hdr:  header,
+			AAAA: net.IPv6zero,
+		}}
+	default:
+		return nil
+	}
+	return resp
+}
+
 func (d *DnsInterceptor) getUpstreamIP(peerKey string) (netip.Addr, error) {
 	peerAllowedIP, exists := d.peerStore.AllowedIP(peerKey)
 	if !exists {
diff --git a/client/server/server.go b/client/server/server.go
index db5909272..b08fc0a03 100644
--- a/client/server/server.go
+++ b/client/server/server.go
@@ -122,7 +122,9 @@ type Server struct {
 	activeCapture    *capture.Session
 	networksDisabled bool
 
-	sleepHandler *sleephandler.SleepHandler
+	sleepHandler  *sleephandler.SleepHandler
+	wakeBarrier   wakeRouteBarrier
+	wakeBarrierMu sync.Mutex
 
 	// mdmTicker periodically re-reads the OS-native MDM policy and triggers
 	// an engine restart when the policy changes. Launched once by Start;
@@ -865,7 +867,11 @@ func (s *Server) Up(callerCtx context.Context, msg *proto.UpRequest) (*proto.UpR
 		}
 		s.mutex.Unlock()
 
-		return s.waitForUp(callerCtx)
+		resp, err := s.waitForUp(callerCtx)
+		if err == nil {
+			s.clearWakeRouteBarrier()
+		}
+		return resp, err
 	}
 	if err := RestoreResidualState(callerCtx, s.profileManager.GetStatePath()); err != nil {
 		log.Warnf(errRestoreResidualState, err)
@@ -966,7 +972,11 @@ func (s *Server) Up(callerCtx context.Context, msg *proto.UpRequest) (*proto.UpR
 	if msg.GetAsync() {
 		return &proto.UpResponse{}, nil
 	}
-	return s.waitForUp(callerCtx)
+	resp, err := s.waitForUp(callerCtx)
+	if err == nil {
+		s.clearWakeRouteBarrier()
+	}
+	return resp, err
 }
 
 // todo: handle potential race conditions
@@ -1191,6 +1201,10 @@ func (s *Server) Down(ctx context.Context, _ *proto.DownRequest) (*proto.DownRes
 
 func (s *Server) cleanupConnection() error {
 	s.oauthAuthFlow = oauthAuthFlow{}
+	s.clearWakeRouteBarrier()
+	if err := cleanupWakeRouteBarrierArtifacts(); err != nil {
+		log.Debugf("cleanup stale wake route killswitch barrier: %v", err)
+	}
 
 	if s.actCancel == nil {
 		return ErrServiceNotUp
@@ -2301,6 +2315,7 @@ func (s *Server) connect(ctx context.Context, config *profilemanager.Config, sta
 	client := internal.NewConnectClient(ctx, config, statusRecorder)
 	client.SetUpdateManager(s.updateManager)
 	client.SetSyncResponsePersistence(s.persistSyncResponse)
+	client.SetRestartHooks(s.protectEngineRestartWithBarrier, s.clearWakeRouteBarrier)
 
 	s.mutex.Lock()
 	s.connectClient = client
diff --git a/client/server/sleep.go b/client/server/sleep.go
index 877ad9690..a94272c1d 100644
--- a/client/server/sleep.go
+++ b/client/server/sleep.go
@@ -4,6 +4,7 @@ import (
 	"context"
 	"os"
 	"strconv"
+	"time"
 
 	log "github.com/sirupsen/logrus"
 
@@ -14,6 +15,15 @@ import (
 
 const envDisableSleepDetector = "NB_DISABLE_SLEEP_DETECTOR"
 
+const (
+	wakeRestartStopTimeout = 15 * time.Second
+	wakeRestartUpTimeout   = 90 * time.Second
+)
+
+type wakeRouteBarrier interface {
+	Close()
+}
+
 // serverAgent adapts Server to the handler.Agent and handler.StatusChecker interfaces
 type serverAgent struct {
 	s *Server
@@ -38,6 +48,10 @@ func (a *serverAgent) Status() (internal.StatusType, error) {
 // logs a warning and returns. Setting NB_DISABLE_SLEEP_DETECTOR=true skips
 // registration entirely.
 func (s *Server) startSleepDetector() {
+	if err := cleanupWakeRouteBarrierArtifacts(); err != nil {
+		log.Debugf("cleanup stale wake route killswitch barrier: %v", err)
+	}
+
 	if sleepDetectorDisabled() {
 		log.Info("sleep detection disabled via " + envDisableSleepDetector)
 		return
@@ -52,15 +66,10 @@ func (s *Server) startSleepDetector() {
 	err = svc.Register(func(event sleep.EventType) {
 		switch event {
 		case sleep.EventTypeSleep:
-			log.Info("handling sleep event")
-			if err := s.sleepHandler.HandleSleep(s.rootCtx); err != nil {
-				log.Errorf("failed to handle sleep event: %v", err)
-			}
+			log.Info("sleep event detected; keeping agent up to preserve routing")
 		case sleep.EventTypeWakeUp:
-			log.Info("handling wakeup event")
-			if err := s.sleepHandler.HandleWakeUp(s.rootCtx); err != nil {
-				log.Errorf("failed to handle wakeup event: %v", err)
-			}
+			log.Info("wakeup event detected; restarting agent with route killswitch barrier")
+			go s.restartAfterWakeWithBarrier()
 		}
 	})
 	if err != nil {
@@ -91,3 +100,96 @@ func sleepDetectorDisabled() bool {
 	}
 	return disabled
 }
+
+func (s *Server) restartAfterWakeWithBarrier() {
+	s.mutex.Lock()
+	if !s.clientRunning || s.actCancel == nil {
+		s.mutex.Unlock()
+		log.Info("skipping wake restart because agent is not running")
+		return
+	}
+	s.mutex.Unlock()
+
+	barrier, ok := s.installWakeRouteBarrier()
+	if !ok {
+		log.Warn("skipping wake restart because route killswitch barrier is unavailable")
+		return
+	}
+	if barrier != nil {
+		s.setWakeRouteBarrier(barrier)
+	}
+
+	s.mutex.Lock()
+	if !s.clientRunning || s.actCancel == nil {
+		s.mutex.Unlock()
+		log.Info("skipping wake restart because agent stopped before restart")
+		s.clearWakeRouteBarrier()
+		return
+	}
+
+	giveUpChan := s.clientGiveUpChan
+	s.actCancel()
+	s.mutex.Unlock()
+
+	if giveUpChan != nil {
+		select {
+		case <-giveUpChan:
+			log.Debug("previous client goroutine stopped after wake restart request")
+		case <-time.After(wakeRestartStopTimeout):
+			log.Warn("timeout waiting for previous client goroutine during wake restart")
+		}
+	}
+
+	ctx, cancel := context.WithTimeout(s.rootCtx, wakeRestartUpTimeout)
+	defer cancel()
+
+	if _, err := s.Up(ctx, &proto.UpRequest{}); err != nil {
+		log.Errorf("wake restart failed, keeping route killswitch barrier active: %v", err)
+		return
+	}
+
+	log.Info("wake restart completed")
+}
+
+func (s *Server) protectEngineRestartWithBarrier(reason string) bool {
+	s.mutex.Lock()
+	running := s.clientRunning && s.actCancel != nil
+	s.mutex.Unlock()
+
+	if !running {
+		log.Debugf("route killswitch barrier skipped for %s: agent is not running", reason)
+		return true
+	}
+
+	barrier, ok := s.installWakeRouteBarrier()
+	if !ok {
+		log.Warnf("route killswitch barrier unavailable for %s", reason)
+		return false
+	}
+	if barrier != nil {
+		s.setWakeRouteBarrier(barrier)
+	}
+	return true
+}
+
+func (s *Server) setWakeRouteBarrier(barrier wakeRouteBarrier) {
+	s.wakeBarrierMu.Lock()
+	oldBarrier := s.wakeBarrier
+	s.wakeBarrier = barrier
+	s.wakeBarrierMu.Unlock()
+
+	if oldBarrier != nil {
+		oldBarrier.Close()
+	}
+}
+
+func (s *Server) clearWakeRouteBarrier() {
+	s.wakeBarrierMu.Lock()
+	barrier := s.wakeBarrier
+	s.wakeBarrier = nil
+	s.wakeBarrierMu.Unlock()
+
+	if barrier != nil {
+		barrier.Close()
+	}
+}
